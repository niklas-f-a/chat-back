export enum ClientTokens {
  AUTH = 'AUTH_CLIENT',
  USER = 'USER_CLIENT',
  CHAT = 'CHAT_CLIENT',
}

export enum RabbitQueue {
  AUTH = 'auth',
  USER = 'user',
  CHAT = 'chat',
}

export enum ServiceTokens {
  AUTH = 'AUTH_SERVICE',
  USER = 'USER_SERVICE',
  CHAT = 'CHAT_SERVICE',
}
